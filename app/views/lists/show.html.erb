<div class = "container">
<p id="notice"><%= notice %></p>

<h3>Here's your <%= @list.title %> list for your trip to <%= @list.description %>!</h3>



<h3>You need to pack:</h3><br />
<div class = "row">
	<% @list.items.each do |item| %>
    <div class = "col-xs-6 col-md-2"><%= item.name %> </div>
	<div class = "col-xs-3 col-md-1">
    <div class="input-group">
        <input type="checkbox" class="btn btn-large">
  </div>
</div>
<br /><br />
<% end %>
<br />


<div class = "container">
<%= link_to 'Add Item', new_list_item_path(@list) %> | 
<%= link_to 'Edit Items', list_items_path(@list) %> | <%= link_to 'Edit List Title', edit_list_path(@list) %> |
<%= link_to 'Back to My Lists', lists_path %> 
</div>
</div>

